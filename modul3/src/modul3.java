public class modul3 {


    public static void main(String[] args) {
        String a = "hello";
        System.out.println(a);
    }
}
    public String[] changeElectResultAgain(String[] results){
        String[] copy = Arrays.copyOf(results, results.length);
        return new int[] {age1, age2, age3, age4};

        tring[] copy = Arrays.copyOf(results, results.length);

        public String[] changeElectResultAgain(String[] results){
            String[] copy = Arrays.copyOf(results, results.length +2);
            return copy;







            Scanner scanner= new Scanner(System.in);
            String i = scanner.nextLine();
            System.out.println(i);


            public void printKeyboard(){
                char[][]arr2 = Arrays.copyOf(createKeyboard(),createKeyboard().length);
                for (int i = 0; i < arr2.length; i++) {
                    System.out.println(Arrays.toString(arr2[i]));
                }
            }

            Scanner scanner = new Scanner(System.in);
            String name  = scanner.nextLine();
            String age = scanner.nextLine();
            String planet  = scanner.nextLine();